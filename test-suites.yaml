version: "3"
services:
  vendor-portal:
    image: stonzim/selenium:latest
    environment:
    - BROWSER
    - HUB_HOST=hub    
    - THREAD_COUNT
    - TEST_SUITE
    volumes:
    - ./output/${TEST_SUITE}:/home/selenium-docker/test-output